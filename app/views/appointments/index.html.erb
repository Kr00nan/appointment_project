<h3>Welcome, <%= current_user.full_name %>!</h3>
<%= link_to "Log Out", destroy_user_session_path, method: :delete %>
<hr />
<h3><%= current_user.appointments.length <= 0 ? "You currently don't have any appointments scheduled." : "" %></h3>

<ul>
  <% @appointments.each do |appointment| %>
    <li>You have an appointment on <%= appointment.weekday %> with <%= appointment.doctor.first_name %>. 
    <%= link_to 'Cancel', appointment, method: :delete %></li>
  <% end %>
</ul>
<br>
<%= link_to "Add an Appointment?", new_appointment_path %>